{
  "name": "voice-editor",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail --names \"backend,frontend\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run start --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "test": "concurrently --names \"backend,frontend\" \"npm test --prefix backend\" \"npm test --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
